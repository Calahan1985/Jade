extends ../layouts/default.jade

block header
block content
  include ../data/array-of-objects
  .row
    each house in arrayOfHouses
      .medium-4.column.content.objectPart
        img(src="#{house.pic}")
        h3 #{house.title}
        p #{house.adress}
        p #{house.describes}

